# Minimal dependencies for RAG_best_practices (Kaggle-friendly)
# Focused set aligned with common Kaggle wheels to avoid resolver conflicts.

# Core
torch==2.5.1
transformers==4.46.2
sentence-transformers==3.3.0
safetensors==0.4.5
accelerate

# Retrieval and NLP
# Prefer GPU FAISS on Kaggle; switch to faiss-cpu if running CPU-only.
faiss-gpu
spacy==3.7.6

# Data and utilities
pandas==2.2.3
numpy==1.26.4
scikit-learn==1.5.2
tqdm==4.67.0

# Evaluation and datasets
mauve-text==0.4.0
rouge-score==0.1.2
datasets==2.19.1

# Topic modeling helpers
gensim==4.3.3
